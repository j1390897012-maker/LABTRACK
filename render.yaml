services:
  - type: web
    name: labtrack-api
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: labtrack-db
          property: connectionString

databases:
  - name: labtrack-db
    plan: free